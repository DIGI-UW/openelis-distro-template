org.openelisglobal.facility.id={{ facility_id }}
org.openelisglobal.country={{ context_name }}
org.openelisglobal.state=
org.openelisglobal.district=

# Sourced from the FHIRSTORE_PASSWORD env var (set in .env, passed through
# docker-compose to the oe.openelis.org container). Rotate by editing .env
# and restarting the container — never write the literal value here.
org.openelisglobal.fhirstore.password=${FHIRSTORE_PASSWORD}
