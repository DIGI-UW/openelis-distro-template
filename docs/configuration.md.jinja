# Configuration Surfaces

`context/config-surfaces.yml` is the source of truth for configurable files. The
contextualization CLI reads the same manifest as this documentation so walkthrough
order and validation stay aligned.

Run `python3 tools/contextualize/oe_context.py doctor` after edits.
